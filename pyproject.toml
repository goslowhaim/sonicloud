[project]
name = "sonicloud"
version = "0.1.0"
description = "Offline harness for Android app market opportunity research."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
sonicloud-harness = "sonicloud_harness.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
